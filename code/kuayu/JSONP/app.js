const data={
    name:'cjs-shuai'
};
// console.log(data);
//处理函数
/* function handle(data){
    //获取result元素
    const result=document.getElementById('result');
    result.innerHTML=data.name;
} */

handle(data);